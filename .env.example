# Copy this file to `.env` and fill in your key.
# Do NOT put a real key in this example file or commit `.env` to git.
#
# Create a key: https://platform.openai.com/api-keys

OPENAI_API_KEY=replace-with-your-key-here

# Optional: model (default is gpt-4o-mini)
# OPENAI_MODEL=gpt-4o-mini
